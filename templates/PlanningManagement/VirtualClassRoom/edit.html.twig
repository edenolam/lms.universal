{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}  
<style> 
.dropdown-menu {
    background-color: #f5f5f5;
} 
</style>  
{% endblock %}

{% block body %}
<div class="lms-breadcrumb">
    <span><a href="
		{% if is_granted('ROLE_APPRENANT') %} {{ path('home_dashboard') }}
        {% elseif is_granted('ROLE_TUTEUR') %} {{ path('dashboard_tuteur') }}
        {% elseif is_granted('ROLE_CONCEPTEUR') %} {{ path('sondage_view') }}
        {% elseif is_granted('ROLE_ADMIN') %} {{ path('admin_user_index') }}
        {% else %} {{ path('home_dashboard') }}
        {% endif %}">{{lms_name}}</a></span> > <span><a href="{{path('admin_virtual_class_room_index')}}">{{ 'menu.virtual_class_room.title' | trans }}</a> </span> > <span class="bold">{{ 'virtual_class_room.action.edit' | trans }} </span>
</div>

<div class="row row-cards">

  <div class="col col-12"> 
    <div>
    	<div class="d-flex align-items-center lms-title">
		<h1 class="text-uppercase lms-h1">{{ 'virtual_class_room.action.edit' | trans }}</h1>
            <hr/>
    	</div>	
    	<form action="{{ path('admin_virtual_class_room_edit', {'id': virtual_class_room.id}) }}" method="post" class="form-horizontal choices" enctype="multipart/form-data">
            <div class="card-body">	
    		  {{ include('PlanningManagement/VirtualClassRoom/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
    	</form>
    </div> 
  </div>

</div> 
    {#<a href="{{ path('admin_virtual_class_room_index') }}">back to list</a>
    {{ include('PlanningManagement/VirtualClassRoom/_delete_form.html.twig') #}

{% endblock %}


{% block js %}
    {{ parent() }} 

<script>  
$().ready(function() {

});
</script>

{% endblock %}